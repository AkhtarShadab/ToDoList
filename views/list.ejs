<%- include("header") -%>
<div class="box" id="heading">
  <h1><%= listTitle %> List</h1>
</div>
<div class="box">
  <% if (newListItem.length ===0) { %> <% } else { %> <%
  newListItem.forEach((element,index) => { %>
  <form action="/delete" method="post">
    <div class="item">
      <input
        type="checkbox"
        onchange="this.form.submit()"
        name="checkbox"
        value="<%= element.id%>"
      />
      <p><%= element.name %></p>
    </div>
    <input type="hidden" name="ListType" value="<%= listTitle %>" />
  </form>
  <% }) %> <% } %>
  <form action="/" method="post" class="item">
    <input type="text" name="Todo" placeholder="New Item" autocomplete="off" />
    <button type="submit" name="list" value="<%=listTitle%>">+</button>
  </form>
</div>
<%- include("footer") -%>
